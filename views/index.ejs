<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Tasks</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Task Management Application</h1>
    <div class="container">
    <% for(task of Tasks){ %>
        
        <div class="task">
            <h2 class="name">Taskname: <%= task.name %></h2>
            <h3 class="description">Description: <%= task.description %></h3>
            <div class="btns">
                <button class="taskBtn"><a href="http://localhost:8080/tasks/<%= task.id %>">See in Detail</a></button>
                <button class="taskBtn"><a href="http://localhost:8080/tasks/<%= task.id %>/edit">Edit</a></button>
                <form method="post" action="/tasks/<%= task.id %>?_method=Delete">
                    <button class="taskBtn">Delete</button>
                </form>
            </div>
        </div>
    <% } %>
    </div>
    <br>
    <div class="btnBox">
        <button class="btn"><a href="http://localhost:8080/tasks/new">Create New Task</a></button>
    </div>
</body>
</html>